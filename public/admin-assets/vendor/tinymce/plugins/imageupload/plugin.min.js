tinymce.PluginManager.add('imageupload', function(editor, url) {
	
	editor.addButton('imageupload', {
		tooltip: 'Transférer une image',
		icon : 'image',
		text: 'Transférer',
		onclick: function() {
			editor.windowManager.open({
			title: 'Transférer une image/video',

			//change this route to the one that returns the '_image-dialog.blade.php' file as a view
			file : global.url.base+'/filemanager/upload',
			width : 360,
			height: 310,
			buttons: [
			{
				text: 'Fermer',
				onclick: 'close'
			}]
			});
		}
	});
});